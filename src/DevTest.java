import java.util.Scanner;

public class DevTest
{
    public static void main(String[] args)
    {
        //Scanner in = new Scanner(System.in);
       // SafeInput.getInt(in, "Enter an Int");

       // Scanner in1 = new Scanner(System.in);
      //  SafeInput.getDouble(in1, "Please enter a double: ");

      //  Scanner in2 = new Scanner(System.in);
      //  SafeInput.getRangedInt(in2, "Please enter an int", 1, 10);

       // Scanner in3 = new Scanner(System.in);
      //  SafeInput.getRangedDouble(in3, "Please enter a double", 1.2, 10.9);


       // Scanner in4 = new Scanner(System.in);
       // SafeInput.getYNConfirm(in4, "Play again? [Y/N]");


      //  Scanner in5 = new Scanner(System.in);
       // String SSN;
        //for some reason "\\d(3)-\\d(2)-\\d(4)" was not working, so I had to do some googling to figure out what would work. This one also has more parameters for what doesn't work!
      //  SSN = SafeInput.getRegExString(in5, "Enter your SSN", "^(?!000|666)[0-8][0-9]{2}-(?!00)[0-9]{2}-(?!0000)[0-9]{4}$");
       // System.out.println(SSN);
    }

    }

